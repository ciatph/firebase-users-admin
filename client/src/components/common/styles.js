const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
    '& .MuiTextField-root, button': {
      marginTop: (theme) => theme.spacing(2)
    }
  }
}

export default styles
