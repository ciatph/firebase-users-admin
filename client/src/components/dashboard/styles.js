const styles = {
  card: {
    marginTop: (theme) => theme.spacing(2)
  },
  cardcontent: {
    width: '100%',
    display: 'flex',
    justifyContent: 'space-between',
    '& span': {
      fontSize: '14px'
    }
  }
}

export default styles
